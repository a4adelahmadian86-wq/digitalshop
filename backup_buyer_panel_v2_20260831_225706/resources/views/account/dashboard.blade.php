@extends('account.layout')
@section('title','داشبورد')
@section('content')
<div class="account-head"><div><small>حساب کاربری</small><h1>سلام {{ $user->first_name ?: 'دوست عزیز' }} 👋</h1><p>وضعیت خریدها و دسترسی‌های شما در یک نگاه.</p></div></div>
<div class="account-stats"><a href="{{ route('account.orders') }}"><small>همه سفارش‌ها</small><strong>{{ number_format($ordersCount) }}</strong></a><a href="{{ route('account.orders', ['status'=>'paid']) }}"><small>سفارش‌های پرداخت‌شده</small><strong>{{ number_format($paidOrdersCount) }}</strong></a><a href="{{ route('account.files') }}"><small>فایل‌های من</small><strong>{{ number_format($downloadsCount) }}</strong></a><a href="{{ route('account.wallet') }}"><small>موجودی کیف پول</small><strong>{{ number_format((int)($user->wallet?->balance ?? 0)) }} <em>تومان</em></strong></a></div>
<div class="account-grid"><section class="account-card"><div class="card-title"><div><small>آخرین سفارش‌ها</small><h2>خریدهای اخیر</h2></div><a href="{{ route('account.orders') }}">همه سفارش‌ها</a></div><div class="order-list">@forelse($recentOrders as $order)<a class="order-row" href="{{ route('account.orders.show',$order) }}"><span><b>{{ $order->order_number }}</b><small>{{ optional($order->created_at)->format('Y/m/d') }}</small></span><span><b>{{ number_format($order->total) }} تومان</b><small>{{ $order->status }}</small></span></a>@empty<div class="empty">هنوز سفارشی ثبت نشده است.</div>@endforelse</div></section><section class="account-card welcome"><div class="welcome-icon">✓</div><h2>دسترسی‌های شما آماده است</h2><p>پس از تکمیل خرید، فایل‌های دیجیتال از بخش فایل‌های خریداری‌شده در دسترس شما قرار می‌گیرند.</p><a class="btn primary" href="{{ route('account.files') }}">مشاهده فایل‌ها</a></section></div>
@endsection
