<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Tax
    |--------------------------------------------------------------------------
    |
    | نرخ مالیات به صورت درصد نگهداری می‌شود تا با تغییر مقررات
    | نیازی به تغییر منطق Checkout نباشد.
    |
    */

    'enabled' => env(
        'DIGITALSHOP_TAX_ENABLED',
        true
    ),

    'rate' => (float) env(
        'DIGITALSHOP_TAX_RATE',
        0
    ),

    /*
    |--------------------------------------------------------------------------
    | Calculation
    |--------------------------------------------------------------------------
    |
    | مالیات فقط یک بار روی مبلغ مشمول محاسبه می‌شود.
    |
    */

    'round' => true,

];