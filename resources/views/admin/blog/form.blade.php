@extends('admin.layout')
@section('content')<div class="admin-content"><h1>{{ $post->exists?'ویرایش مقاله':'مقاله جدید' }}</h1><form class="editor-card" method="POST" action="{{ $post->exists?route('admin.blog.update',$post):route('admin.blog.store') }}">@csrf @if($post->exists) @method('PUT') @endif<label>عنوان<input name="title" value="{{ $post->title }}" required></label><label>Slug<input name="slug" value="{{ $post->slug }}" placeholder="خالی = ساخت خودکار"></label><label>خلاصه<textarea name="excerpt" rows="4">{{ $post->excerpt }}</textarea></label><label>محتوا<textarea name="content" rows="25" required>{{ $post->content }}</textarea></label><label class="check"><input type="checkbox" name="is_published" value="1" @checked($post->is_published)> انتشار</label><button>ذخیره</button></form></div>@endsection
@push('styles')<style>.admin-content{padding:24px}.editor-card{max-width:900px;background:#fff;border:1px solid #eaecf0;border-radius:16px;padding:20px;display:grid;gap:13px}.editor-card label{display:grid;gap:6px;font-size:11px;font-weight:800}.editor-card input,.editor-card textarea{font:inherit;border:1px solid #d0d5dd;border-radius:10px;padding:10px}.editor-card textarea{line-height:1.9}.editor-card .check{display:flex;align-items:center;gap:7px}.editor-card button{border:0;background:#111827;color:#fff;border-radius:10px;padding:11px;cursor:pointer}</style>@endpush
