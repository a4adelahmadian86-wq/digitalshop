@extends('admin.layout')
@section('title','دسته‌بندی‌ها')
@section('content')
<div class="admin-page"><div class="admin-page-head"><div><div class="admin-eyebrow">مدیریت فروشگاه</div><h1>دسته‌بندی‌ها</h1><p>۲ سطح دسته‌بندی، نامک انگلیسی، ترتیب نمایش و وضعیت فعال.</p></div><a href="{{ route('admin.categories.create') }}" class="admin-primary-btn">+ افزودن دسته‌بندی</a></div>
@if(session('success'))<div class="admin-alert success">{{ session('success') }}</div>@endif @if(session('error'))<div class="admin-alert error">{{ session('error') }}</div>@endif
@if($categories->isEmpty())<section class="admin-box admin-empty"><h2>هنوز دسته‌بندی‌ای ایجاد نشده است</h2><p>دسته‌بندی اصلی یا زیرگروه جدید ایجاد کنید.</p><a href="{{ route('admin.categories.create') }}" class="admin-primary-btn">افزودن دسته‌بندی</a></section>@else
<section class="admin-table-card"><div class="admin-table-head"><h2>فهرست دسته‌بندی‌ها</h2><span>{{ number_format($categories->count()) }} دسته</span></div><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>دسته</th><th>والد</th><th>Slug</th><th>محصولات</th><th>سطح</th><th>وضعیت</th><th>عملیات</th></tr></thead><tbody>@foreach($categories as $category)<tr><td><strong>{{ $category->level>0?'— ':'' }}{{ $category->name }}</strong></td><td>{{ $category->parent?->name ?: 'دسته اصلی' }}</td><td><span class="discount-code">{{ $category->slug }}</span></td><td>{{ number_format($category->products_count) }}</td><td>{{ $category->level===0?'اصلی':'زیرگروه' }}</td><td>@if($category->is_active&&$category->status)<span class="status-badge active">فعال</span>@else<span class="status-badge inactive">غیرفعال</span>@endif</td><td><div class="admin-actions"><a href="{{ route('admin.categories.edit',$category) }}" class="action-btn edit">ویرایش</a><form method="POST" action="{{ route('admin.categories.toggle',$category) }}">@csrf @method('PATCH')<button type="submit" class="action-btn toggle">{{ $category->is_active?'غیرفعال کردن':'فعال کردن' }}</button></form>@if($category->products_count===0&&!$category->children()->exists())<form method="POST" action="{{ route('admin.categories.destroy',$category) }}" onsubmit="return confirm('آیا از حذف این دسته‌بندی مطمئن هستید؟');">@csrf @method('DELETE')<button type="submit" class="action-btn delete">حذف</button></form>@else<span class="status-badge warning">دارای وابستگی</span>@endif</div></td></tr>@endforeach</tbody></table></div></section>@endif</div>
@endsection