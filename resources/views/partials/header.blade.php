@php
$cart=session('cart',[]);$cartProducts=empty($cart)?collect():\App\Models\Product::whereIn('id',array_keys($cart))->get();$cartTotal=$cartProducts->sum('price');
$icon=function($path){return '<svg class="ds-site-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">'.$path.'</svg>';};
$cartIcon=$icon('<path d="M4 5h2l1.4 9.2a2 2 0 0 0 2 1.7h7.9a2 2 0 0 0 1.9-1.4L21 8H7"/><circle cx="10" cy="19" r="1.2"/><circle cx="18" cy="19" r="1.2"/>');
$walletIcon=$icon('<path d="M4 7h15a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V7a2 2 0 0 1 2-2h12"/><path d="M16 12h3"/>');
@endphp
<header class="header"><div class="container nav"><a class="logo" href="{{ route('home') }}">فایل‌مارکت</a><nav><ul class="menu"><li><a href="{{ route('home') }}">خانه</a></li><li><a href="{{ route('products.index') }}">فروشگاه</a></li><li><a href="#">دسته‌بندی‌ها</a></li><li><a href="#">وبلاگ</a></li></ul></nav><div class="nav-actions"><button class="cart-trigger" onclick="toggleCart()"><span>{!! $cartIcon !!}</span><span>سبد</span><b>{{ array_sum($cart) }}</b></button>@auth @include('partials.notification-center')<a class="wallet-header-link" href="{{ route('wallet.index') }}"><span class="wallet-header-icon">{!! $walletIcon !!}</span><span>کیف پول</span>@php($headerWallet=auth()->user()->wallet)@if($headerWallet)<b>{{ number_format($headerWallet->balance) }}</b>@endif</a><form method="POST" action="{{ route('logout') }}">@csrf<button class="login">خروج</button></form>@else<a class="login" href="{{ route('login') }}">ورود</a><a class="register-link" href="{{ route('register') }}">ثبت‌نام</a>@endauth</div></div></header>
<aside id="cart-panel" class="cart-panel"><div class="cart-head"><strong>سبد خرید</strong><button onclick="toggleCart()">×</button></div><div class="cart-items">@forelse($cartProducts as $product)<div class="mini-cart"><span>{{ $product->title }}</span><strong>{{ number_format($product->price) }}</strong></div>@empty<div class="empty">سبد خرید خالی است.</div>@endforelse</div>@if($cartProducts->count())<div class="cart-total">مجموع: {{ number_format($cartTotal) }} تومان</div><a class="checkout-btn" href="{{ route('cart') }}">مشاهده سبد خرید</a>@endif</aside><div id="cart-overlay" onclick="toggleCart()"></div>
@push('styles')<style>.ds-site-icon{width:1.15em;height:1.15em;display:inline-block;vertical-align:-.18em}.cart-trigger,.wallet-header-icon{display:inline-flex;align-items:center;gap:6px}.cart-trigger .ds-site-icon,.wallet-header-icon .ds-site-icon{width:18px;height:18px}.wallet-header-icon{justify-content:center}</style>@endpush