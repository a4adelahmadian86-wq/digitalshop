document.addEventListener('DOMContentLoaded',()=>{const sort=(selector,itemSelector,score)=>{const box=document.querySelector(selector);if(!box)return;const list=box.closest('.p-feedback-card')?.querySelector(itemSelector+'-list');if(!list)return;const buttons=[...box.querySelectorAll('span')];buttons.forEach((b,i)=>b.addEventListener('click',()=>{buttons.forEach(x=>x.classList.remove('active'));b.classList.add('active');const items=[...list.querySelectorAll(itemSelector)];if(i===0)items.reverse();else items.sort((a,z)=>score(z)-score(a));items.forEach(x=>list.appendChild(x))}))};sort('.p-sort','.p-review',el=>{const s=el.querySelector('.p-review-top span');return s?(s.textContent.match(/★/g)||[]).length:0});sort('.p-question-sort','.p-question',el=>el.querySelectorAll('.p-answer').length)});