document.addEventListener('DOMContentLoaded',()=>{const menu=document.querySelector('.dj-category');if(!menu)return;const trigger=menu.querySelector(':scope>a'),panel=menu.querySelector(':scope>.dj-mega');if(!trigger||!panel)return;let openTimer=null,closeTimer=null;const desktop=()=>window.matchMedia('(min-width:901px)').matches;const open=()=>{clearTimeout(closeTimer);menu.classList.add('is-open');trigger.setAttribute('aria-expanded','true')};const close=()=>{clearTimeout(openTimer);clearTimeout(closeTimer);menu.classList.remove('is-open');trigger.setAttribute('aria-expanded','false')};const delayedOpen=()=>{if(!desktop())return;clearTimeout(closeTimer);clearTimeout(openTimer);openTimer=setTimeout(open,650)};const delayedClose=()=>{if(!desktop())return;clearTimeout(openTimer);closeTimer=setTimeout(close,180)};trigger.setAttribute('aria-expanded','false');trigger.addEventListener('click',e=>{e.preventDefault();if(desktop()){menu.classList.contains('is-open')?close():open()}else{menu.classList.contains('is-open')?close():open()}});menu.addEventListener('mouseenter',delayedOpen);menu.addEventListener('mouseleave',delayedClose);panel.addEventListener('mouseenter',()=>clearTimeout(closeTimer));panel.addEventListener('mouseleave',delayedClose);document.addEventListener('click',e=>{if(!menu.contains(e.target))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});window.addEventListener('resize',()=>{if(!desktop())clearTimeout(openTimer)});});